
export const DAILY_VIEW = "DAILY_VIEW";
export const WEEKLY_VIEW = "WEEKLY_VIEW";

export const changeCalendarFormat = (type) => (dispatch) => {
    return dispatch({ type });
};

export function changexcv(type) {
    return { type };
}